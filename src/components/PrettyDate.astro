---
type Props = {
    date: string
}

const isoDateString = new Date(Astro.props.date).toISOString()
const formattedDate = new Date(Astro.props.date).toLocaleDateString('en-GB', {
    dateStyle: 'long'
})

---

<time datetime={isoDateString} class="date">{formattedDate}</time>